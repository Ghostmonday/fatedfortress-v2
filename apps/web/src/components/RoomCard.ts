/**
 * components/RoomCard.ts — Room card for the browse table.
 *
 * Displays:
 *   - Category glyph: { } $ % # @ etc.
 *   - Name + truncated description
 *   - @N participants (presence via Y.js)
 *   - Fuel gauge bar (for pooled rooms)
 *   - Price badge (for paid rooms)
 *   - JOIN button → navigates to /#rm_{id}
 *
 * Brutalist style: 1px black border, no radius, offset shadow.
 */

// TODO: components/RoomCard.ts
