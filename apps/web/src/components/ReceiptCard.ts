/**
 * components/ReceiptCard.ts — Receipt display card for /me page.
 *
 * Shows:
 *   - Receipt ID + timestamp
 *   - Model used + prompt preview
 *   - Output hash
 *   - Fork count + parent chain breadcrumb
 *   - FORK button → creates new room pre-seeded with this receipt
 *   - here.now URL copy button
 *
 * Fork graph rendered as ASCII tree: ReceiptCard → ReceiptCard → ... → root
 */

// TODO: components/ReceiptCard.ts
