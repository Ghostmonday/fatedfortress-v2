/**
 * router.ts — Hash-based SPA router.
 *
 * Routes:
 *   /                  → room table (browse)
 *   /#rm_{id}         → live room
 *   /connect           → living docs page
 *   /me                → receipts vault
 *
 * Uses hash routing so every route is a shareable URL with no server config.
 */

// TODO: router.ts
