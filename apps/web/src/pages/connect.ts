/**
 * pages/connect.ts — Living docs (/connect).
 *
 * Interactive API reference. Each provider block:
 *   - Inline <textarea> for pasting a key
 *   - ">>>" button executes the request in-browser against the real API
 *   - Response streams inline below the code block
 *   - "Convert to room" button pre-fills a room with that provider
 *
 * Key validation runs inside the Fortress Worker — key never touches main thread.
 * Worker SRI hash displayed so users can verify what script loaded.
 */

// TODO: pages/connect.ts
