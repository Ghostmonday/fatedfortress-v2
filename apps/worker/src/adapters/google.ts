/**
 * adapters/google.ts — Google Gemini API adapter.
 *
 * API style: Google REST API, streaming via text/event-stream
 * Endpoint: https://generativelanguage.googleapis.com/v1beta/models/{model}:streamGenerateContent
 * Models: gemini-2.0-flash, gemini-2.0-pro
 *
 * Auth: Bearer token in Authorization header.
 * Request body: JSON { contents: [{ parts: [{ text }] }], systemInstruction }
 */

// TODO: adapters/google.ts
