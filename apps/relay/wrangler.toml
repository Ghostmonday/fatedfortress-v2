# apps/relay/wrangler.toml
# Cloudflare Worker (Durable Object) for WebRTC signaling relay
#
# Deploy: wrangler deploy
# Local dev: wrangler dev

name = "ff-relay"
main = "src/index.ts"
compatibility_date = "2026-01-01"

[observability]
enabled = false

# TODO: Fill in account_id after `wrangler login`
# account_id = ""

# TODO: Durable Object binding — set SCRIPT_NAME to the relay script name
# [[durable_objects.bindings]]
# name = "SIGNALING"
# class_name = "SignalingDO"

[vars]
# Env vars injected at deploy time, not committed
# FF_ORIGIN = "https://fatedfortress.com"
