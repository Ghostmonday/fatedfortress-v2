/**
 * packages/ui-brutalist/src/index.ts — Brutalist design tokens and primitives.
 *
 * Design tokens (CSS custom properties + TypeScript constants):
 *   Colors: --ff-bg (#fff), --ff-ink (#000), --ff-muted (#666)
 *   Typography: --ff-font (JetBrains Mono, Google Fonts)
 *   Scale: --ff-12, --ff-24, --ff-36, --ff-48, --ff-96
 *   Spacing: --ff-8, --ff-16, --ff-24
 *   Border: --ff-border (1px solid #000)
 *   Shadow: --ff-shadow (2px 2px 0 #000)
 *
 * Primitive components:
 *   Button (invert on hover, black/white only)
 *   Input (bordered, no radius, monospace)
 *   Panel (bordered container with label)
 *   Table (dense, bordered rows)
 *   Glyph (renders giant ASCII chars as background elements)
 *
 * All values are hardcoded: #fff, #000, #666 — NO exceptions.
 * No runtime theming. No CSS variables for colors.
 */

// TODO: packages/ui-brutalist/src/index.ts
